<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:WeberTech.LicenseGenerator.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="900" d:DesignHeight="700"
             x:Class="WeberTech.LicenseGenerator.Views.LoginView"
             x:DataType="vm:LoginViewModel">

  <Design.DataContext>
    <vm:LoginViewModel />
  </Design.DataContext>

  <Grid>
    <Border Classes="card" Width="380" HorizontalAlignment="Center" VerticalAlignment="Center">
      <StackPanel Spacing="16">

        <StackPanel Spacing="4" HorizontalAlignment="Center" Margin="0,0,0,8">
          <TextBlock Classes="headline-md" Text="WeberTech Licensing" HorizontalAlignment="Center" />
          <TextBlock Classes="body-sm" Text="Entrar na ferramenta do emissor" HorizontalAlignment="Center" />
        </StackPanel>

        <StackPanel Spacing="4">
          <TextBlock Classes="label-md" Text="UTILIZADOR" />
          <TextBox Text="{Binding Username}" Watermark="ex.: joao.silva" />
        </StackPanel>

        <StackPanel Spacing="4">
          <TextBlock Classes="label-md" Text="PASSWORD" />
          <TextBox Text="{Binding Password}" PasswordChar="•" />
        </StackPanel>

        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource ErrorBrush}"
                   TextWrapping="Wrap"
                   IsVisible="{Binding ErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />

        <Button Classes="accent"
                Content="Entrar"
                Command="{Binding LoginCommand}"
                IsEnabled="{Binding !IsBusy}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center" />

        <Button Classes="tertiary"
                Content="Ainda não tenho conta — criar"
                Command="{Binding GoToRegisterCommand}"
                HorizontalAlignment="Center" />

      </StackPanel>
    </Border>
  </Grid>
</UserControl>
